# 2026-04-08 工作日志

## Phase 1 完成：Dual-FSM DMA + Tile-Loop Ctrl

### 完成项
1. **重写 `npu_dma.v`**：拆分为 Load-FSM (L_IDLE/L_WREAD/L_AREAD/L_WA_READ) 和 WB-FSM (WB_IDLE/WB_ACTIVE) 双独立状态机，共享 AXI4 端口
2. **重写 `npu_ctrl.v`**：新增 S_CONFIG（锁存维度+发射 DMA）、S_NEXT_TILE（tile 索引推进+终止判断）状态，支持 M×N tile 循环
3. **修复关键 Bug：NBA 延迟** — S_CONFIG 中 `dma_w_len <= n_dim_r * k_dim_r` 读到旧值 x，改为直接用输入 wire
4. **验证通过** — Phase 1 改动后 N4 测试结果与 HEAD 完全一致（0/4 PASS 为预存 bug）

### 调试经验总结
- **wb_wait_cnt 未初始化导致 S_DONE 死锁** → reg 必须在 reset 块中初始化为 0
- **m_axi_wlast/m_axi_wdata 声明为 reg 但用 assign 驱动** → 必须改为 wire 或 procedural assignment
- **Load-FSM 用两阶段 AR 握手 vs HEAD 的单周期 arvalid=1** → 简化为与 HEAD 一致的无条件 arvalid 行为
- **`$display` 格式参数数量不匹配** → Verilog $display 不检查参数数量

### 下一步
- 修复预存 bug：PE 结果通路断裂（got=x/0），需要深入追踪 pe_top valid_out → npu_top FIFO 写入路径

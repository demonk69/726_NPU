Archived documents in this directory are historical records, not current guidance.

Use the maintained documents in `doc/` and the top-level `README.md` for current architecture, verification status, and run commands.

Archived files were moved here because they describe older Windows/Icarus flows or pre-VGG worklogs that can mislead future development if read as current state. Obsolete plan documents have been removed from the repository.
